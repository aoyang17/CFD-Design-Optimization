# Data-driven optimization

This directory groups single-fidelity, multifidelity, EGO, and operation-aware data workflows. `Multifidelity_Operation-aware/` is the integration layer: it references the component workflows rather than duplicating their implementations.
