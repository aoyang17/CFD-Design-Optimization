# Geometric parameterization

`FFD/` contains reusable FFD setup and constraint helpers. `Mode/` contains the distinct modal parameterization implementations needed by the CFD and data-driven workflows, organized by their pyGeo/MPhys compatibility context.

Shared CRM files are externally managed assets; use the `crm-v1` manifest rather than adding new duplicate meshes or model bases here.
